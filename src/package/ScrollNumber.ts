import ScrollNumber, {
  ScrollNumberRef,
  ScrollNumberProps,
} from "../Components/ScrollNumber"
import ScrollNumberGroup, {
  ScrollNumberGroupRef,
  ScrollNumberGroupProps,
} from "../Components/ScrollNumberGroup"

export { ScrollNumber, ScrollNumberGroup }
export type {
  ScrollNumberProps,
  ScrollNumberRef,
  ScrollNumberGroupProps,
  ScrollNumberGroupRef,
}
